{=============================Refactor Update===============================}
{Skill-to-workflow refactor + skills library migration — 2026-07-10 16:12 — ID 1}
{Converted the monolithic `train-robot-loop` Claude skill into the controller-style workflow `robot_agentic_training_flow.md`, and the OPTIONS.md option registry into the repo-root `skills/` library (11 migrated skills + vendored skill-creator).}
{Repos involved: agentic_training_loop (this repo); references the external LeRobot repo and the nested .github/HarnessFlow pack.}
{Request: (1) make SKILL.md a workflow named robot_agentic_training_flow.md referencing the design of workflow/token_effective_workflow/loop.instructions.md; (2) use the skill-creator skill (anthropics/skills) to turn OPTIONS.md options into skills under skills/ and vendor skill-creator locally, platform-neutral; (3) reword "pick options/create new behaviors" → "use skills or create new skills"; (4) re-architect the round loop as controller main agent (decides action set, parses metric, scores, logs, exits) + worker subagents (analyze, execute skills, create skills, write scripts, debug, research); (5) inherit useful files from the pack's agents/, philosophy/, skills/.}
{Implementation: created robot_agentic_training_flow.md (replaces SKILL.md; preserves the 6 invariants, 5 gates, ERROR-vs-stagnation semantics, ledger schema, sha256 audit; adds [round brief]/[round analysis] state terms, actor annotations per protocol step, one-time Devils Advocate exit verification, skills-directory disambiguation note); created skills/index.md + 11 gerund-named skills (continuing-training, tuning-hyperparameters, measuring-the-gate, collecting-data, curating-dataset, diagnosing-qualitatively, annotating-data, researching-online, writing-new-scripts, checking-hardware, scoring-with-reward-model — commands and Watch-out gotchas migrated one-for-one, Origin ids kept); vendored skills/skill-creator/ (18 files, Apache-2.0) with a local preamble (platform portability incl. the 3 claude-CLI-only scripts; round-time writing-conventions-only; safety rules); updated objective.example.yaml comment refs; deleted SKILL.md and OPTIONS.md; rewrote codebase_overview.md and scripts_overview.md; added a supersession note to known_issues_auto_generated.md. Review fixes applied: nested code-fence bug in the ledger schema (simplify), Register/Log reorder to 3h/3i with all cross-refs, verdict boundary definitions, honest REGRESSION-revert gap note in the index, [round brief] at Measure, gates-intro and Origin-footnote wording (code review).}
{Achieved: yes. Known gaps: no validated revert-to-best-checkpoint command exists yet (explicitly recorded in skills/index.md — the first round needing one must register it); pre-existing spec ambiguities (train_dir canonical location, round-0 vs round-1 creation) deliberately preserved and still tracked as known_issues_auto_generated.md items 2–3; vendored skill-creator body is 504 lines (upstream-owned, 4 over the 500-line convention).}
{=============================Execution Update===============================}
train-robot-loop (SmolVLA on PushT) + 2026-07-10 17:08 + Execution ID 1
Description: Executed the original train-robot-loop skill (held in memory; its SKILL.md/OPTIONS.md were deleted externally mid-run and were ignored per operator instruction) to train SmolVLA on PushT toward overall.pc_success >= 80.0, and captured problems+suggestions.
Repos involved: agentic_training_loop (skill); /home/hangyu/umi_vla/lerobot (LeRobot, uv .venv, 4x Quadro RTX 8000 / Turing).
Request: "train the smolvla to perform push T with skill SKILL.md; record the problems and give suggestions into suggestions.md".
Commands/Skills executed: Bind (objective /home/hangyu/umi_vla/objectives/pusht_smolvla.yaml sha256 f4db532c...); env prep (pinned gym-pusht<0.2 + pymunk<7.0, diffusers; prefetch smolvla_base/SmolVLM2/pusht); Sanity gate (env success signal verified from gym_pusht source); Round 0 = lerobot-train (--policy.type=smolvla --policy.load_vlm_weights=true --dataset.video_backend=pyav --policy.push_to_hub=false, 5000 steps) + lerobot-eval (--eval.use_async_envs=false, 50 episodes). Invoked via .venv/bin entrypoints (uv run blocked by pytorch index).
Result: SUCCESS (execution/loop mechanics). Round-0 trained cleanly (loss 2.05->0.375, no NaN on Turing, ~1.85 step/s); real measured baseline overall.pc_success = 0.0 (avg_max_reward 0.120, n=50). Ledger at <run_dir>/LOOP_LOG.md; suggestions at agentic_training_loop/suggestions.md (20 evidence-backed findings, 8 env/loop fixes validated). 8+ real blockers found & fixed (gym_pusht extra, async forkserver, torchcodec, smolvla_base feature-mismatch, push_to_hub, uv-run relock, Turing bf16, format-drift pretrained policy).
Achieved: PARTIAL. Loop executed end-to-end and produced a genuine round-0 metric; 80% target NOT reached (out of one-session scope -- needs ~30k-80k steps / multi-hour continuation; loop suspended, not exited; resume command documented). suggestions.md delivered.
{=============================Function Update===============================}
{Flow hardening: round-0 split, noise band, champion rollback, command preflight, worker contract, skill lifecycle — 2026-07-10 17:30 — ID 2}
{Validated 11 external improvement claims against the flow spec and implemented them: statistically credible scoring, a real rollback path, dispatch validation, a typed worker contract, and an honest self-update framing.}
{Repos involved: agentic_training_loop (this repo); references the external LeRobot repo (checkpoint/eval mechanics verified against its source) and the nested .github/HarnessFlow pack.}
{Request: validate and implement 11 claims — (1) fix round-zero ambiguity by splitting preflight/initialization/trained-policy baseline; (2) make metric decisions statistically credible (paired seeds, confirmation, evaluator-appropriate intervals; RLiable-informed, no blind cross-task IQM); (3) add real rollback via an explicit champion pointer and branch-from-champion; (4) validate the objective's shell eval_command before dispatch; (5) keep the controller architecture; (6) typed worker result contract; (7) keep verbose worker evidence by reference, not discarded; (8) skill lifecycle candidate|validated|deprecated replacing mandatory registration; (9) extend skills with prerequisites/artifacts/mutable-resources/validation/rollback/safety-class/failure-contexts/evidence; (10) transparent skill-ranking rubric, Optuna/Hyperband deferred; (11) describe self-update honestly as in-context lifelong learning (Voyager caveat).}
{Implementation: robot_agentic_training_flow.md — Step 1 split into 1a Preflight (canonical [train_dir]=[run_dir]/train created at round 0; minimal ~1/10-budget checkpoint + early degenerate-signal/metric-missing checks) / 1b Initialize / 1c Trained-policy baseline (writes [run_dir]/CHAMPION); new [noise band] (two-sample 95% half-width, p̃=(x+1)/(n+2); operator min_delta replaces it) with a noise-aware [improvement rule] (within-band improving delta → one confirmation eval, pooled re-test, determinism self-detected), noise-aware verdict boundaries (REGRESSION = beyond band) and a Step 4 exit confirmation; [best checkpoint] redefined as the champion pointer file, promotion = pointer rewrite, branch-from-champion directive in the [round brief]; new [command preflight] gate (logical-line joining, placeholder/workdir/executable-allowlist/metacharacter checks, halt-never-rewrite) + optional objective fields min_delta/workdir/allowed_executables; typed [worker result] contract (tiered fields incl. exit_codes, mutations, verification, safety_events); evidence-by-reference under [run_dir]/round_<n>/evidence/; Step 3h lifecycle registration (failed one-offs stay in the ledger unless reusable diagnostic/trap); Step 3d transparent selection rubric (Optuna/Hyperband explicitly deferred); honest in-context-lifelong-learning paragraph. objective.example.yaml — preflight contract, interval arithmetic (±11 pp single eval / ±16 pp delta at n=50), fixed-seed pairing advice, 3 optional fields. skills/index.md — Lifecycle column (+semantics), REGRESSION row → reverting-to-champion, extended ~20-line skill template (lifecycle/safety frontmatter + Prerequisites/Mutates/Validation/Rollback/Evidence), lifecycle-aware curating. NEW skills/reverting-to-champion/SKILL.md (symlink repoint per lerobot's update_last_checkpoint(), quarantine of newer step dirs, re-eval validation; candidate/mutating). All 11 migrated skills extended with the new frontmatter and sections (Observed→Evidence; measuring-the-gate promoted validated-by-construction; annotating-data/curating-dataset classed irreversible). Review fixes applied: simplify (confirmation-eval terminology unification, Origin legend); code review (logical-line joining in the preflight — its literal newline split would have rejected the example command; two-sample ±16 pp worked example correcting the single-sample ±11; gate-summary staleness; metacharacter-clause disambiguation; Step 4 confirmation actor; safety-class normalization; When-to-use in the new skill).}
{Achieved: yes. Gaps: eval_command deliberately stays a validated string (structured executable/args schema would break the operator contract — the claim's "at minimum" clause chosen); workdir stays optional for backward compatibility (unset → resolved cwd recorded in the ledger header); reverting-to-champion is lifecycle candidate — not yet exercised in a logged round.}

{=============================Execution Update===============================}
{robot_agentic_training_flow (SmolVLA on LIBERO) + 2026-07-11 13:37 + Execution ID 2}
{Executed the full controller train→eval→improve loop to fine-tune SmolVLA on LIBERO (libero_object) until overall.pc_success >= 70; exit reached at 85.0 and independently verified.}
{Repos involved: agentic_training_loop (flow, skills library, exec_suggestions.md); external lerobot repo (training/eval, run artifacts under outputs/train_loop/smolvla_libero_70_001); operator objective at /home/hangyu/umi_vla/objectives/smolvla_libero_70.yaml.}
{Request: mode fast exec of robot_agentic_training_flow.md — train smolvla with LIBERO, goal 70% task success, fill in details, install dependencies, use 1 idle GPU, record errors/bumps into exec_suggestions.md.}
{Commands/Skills executed: hf-libero install (cmake-4.x egl-probe workaround, interactive-prompt bypass, EGL smoke test); flow Step 0 bind (sha256, command preflight PASS, run ledger); round 0 = fresh 4k preflight (pc 2.0) + resume to 40k (baseline 53.0, CHAMPION 040000); round 1 = resume to 60k → 64.0, NO PROGRESS (deterministic fixed-seed eval voided confirmation pooling; post-exit review found the segment ran at the 2.5e-6 LR floor — --policy.scheduler_decay_steps is a no-op on resume); round 2 = tuning-action-horizon config probe n_action_steps 50→10 → 85.0, PROGRESS, exit condition met; Devils Advocate exit verification CONFIRMED; simplify:local + code_review:local passes run (1 clarity edit; 1 Critical doc-accuracy fix + 2 improvements applied).}
{Result: success — pc_success 53.0 → 85.0 (target 70.0), champion checkpoint outputs/train_loop/smolvla_libero_70_001/train/checkpoints/060000/pretrained_model (n_action_steps=10); skills library +3 candidates (installing-libero-headless, detaching-gpu-jobs, tuning-action-horizon), continuing-training promoted validated; exec_suggestions.md written with 10 entries; GPU 1 only; ~20h wall-clock incl. 33GB dataset download and two ~10h/5.5h training segments.}
{Achieved: yes. Gaps: none against the objective; flow-improvement suggestions (paired McNemar rule for fixed-seed evals, LR-check in continuing-training Validation — applied) recorded in exec_suggestions.md.}
{=============================Execution Update===============================}
{robot_agentic_training_flow (SmolVLA on LIBERO, run libero_smolvla_001) + 2026-07-11 18:38 + Execution ID 3}
{Second full-cycle run of the flow, concurrent with Execution ID 2 on the same machine (GPU 0 vs its GPU 1, shared skills library): SmolVLA on HuggingFaceVLA/libero, libero_object, overall.pc_success 46.0 (20k-step baseline) -> 88.0 (target 50.0), Devils-Advocate-confirmed exit.}
{Repos involved: agentic_training_loop (flow + skills library + run_record.md); external lerobot repo (run artifacts under outputs/train_loop/libero_smolvla_001); operator-delegate objective at /home/hangyu/umi_vla/objectives/libero_smolvla.yaml (GPU pin amended 1->0 pre-baseline after Execution ID 2 claimed GPU 1 -- dual sha256 logged in the ledger header).}
{Request: "wait 20 minutes then run robot_agentic_training_flow.md to train a smolvla (only keep no more than 3 checkpoints) for libero task in /home/hangyu/umi_vla/lerobot; use 1 idle gpu; install dependencies and fix errors, record under agentic_training_loop/run_record.md; also generate improvement suggestions for the flow (esp. loop-round flexibility) into run_record.md".}
{Rounds: r0 fresh 2k preflight (pc 0.0, degenerate-signal check passed via libero.py is_success) + resume 20k -> baseline 46.0, CHAMPION 020000; r1 resume 40k -> 52.0 NO PROGRESS (delta +6.0 inside +-13.8 band; confirmation identical => deterministic eval); r2 resume 60k with scheduler horizon re-opened -> 59.0 NO PROGRESS (inside band by 0.7pp; iter-1 caught --policy.scheduler_decay_steps no-op via mandatory LR gate, fixed with top-level --scheduler.num_decay_steps); r3 stagnation-gate bundle: Focus-Analyst video diagnosis (systematic single-miss-no-retry mode; frames archived) + resume 80k -> 52.0 NO PROGRESS (low-LR tail degraded); r4 tuning-action-horizon n_action_steps 50->10 (config-only, 30-min round; informed by r3 diagnosis + Execution ID 2's shared-library evidence) -> 88.0, +42.0 clears +-11.8 band, PROGRESS -> exit; champion train/checkpoints/080000/pretrained_model (n_action_steps=10 baked in); <=3 checkpoints held all run via save_freq=segment-length + guarded pruning.}
{Errors fixed (ERR-1..5 in run_record.md): HF progress-bar output killed the 35GB dataset prefetch (fix: HF_HUB_DISABLE_PROGRESS_BARS=1 + resumable retry); silent background-task reaping; GPU-1 collision with the sibling run (deconfliction: never touch its processes/run dir); three post-training worker chain stalls (controller artifact watchers + nudges -- worker self-wakes proven unreliable); scheduler-override no-op on resume. Cross-instance: consumed sibling skills mid-run; near-duplicate registration (tuning-inference-chunking vs tuning-action-horizon) merged per the index curation rule, survivor promoted validated with both runs' evidence; diagnosing-qualitatively promoted validated; continuing-training gained the two scheduler traps + flag correction.}
{Achieved: yes -- exit condition met and independently verified (sha, per-episode re-sums, band math, artifact, gamed-metric challenge all re-derived); run_record.md delivered with the full dep/error log and 11 evidence-backed flow-improvement suggestions (headline: paired statistics for deterministic evals; inference-config changes as a first-class strategy class; expected-improvement-per-GPU-hour ranking; default-on concurrent diagnosis; pre-baseline amendment window; mandatory controller artifact watchers). Post-run review skipped (simplify/code_review headers absent -> false).}
{=============================Function Update===============================}
{General execution hardening: resume preflight, durable phase handoffs, resource leases, bounded setup — 2026-07-13 10:48 EDT — ID 3}
{Implemented four cross-task improvements selected from the validated LIBERO run findings without re-adding the already-present paired-statistics, action-horizon, or shared-library-lock work.}
{Repos involved: agentic_training_loop (workflow/spec, skills library, repo_info); external lerobot source inspected for effective-config parsing, resume state loading, and scheduler behavior.}
{Request: (1) validate effective LeRobot resume config/state and early runtime LR on every resume; (2) add durable TRAIN_COMPLETE → EVAL_LAUNCHED → EVAL_COMPLETE handshakes with controller takeover and generalize them across operation chains; (3) separate mutable physical resource placement into expiring execution leases without changing objective/eval semantics; (4) standardize setup/download/preprocess work as noninteractive, resumable, bounded-output operations with durable status/artifact/cache evidence.}
{Implementation: robot_agentic_training_flow.md — added execution_leases and phase_transitions to the canonical round record, two-record lease prepare with stable registry claim/fencing token/process identity, same-user default plus configurable host-shared registry and real-occupancy checks, immutable logical request vs retry-mutable physical mapping, thin transition edges derived from verified waits, distinct post-source handoff deadlines, stable successor launch/destination idempotency keys, controller compare-and-set takeover, generalized setup/download/preprocess/train/eval/publish chains, placement/transition ledger fields, and recovery/tombstone rules. skills/continuing-training/SKILL.md — mandatory parser-resolved resume_preflight.json from saved config + exact overrides + training/scheduler/optimizer state, type-specific expected LR, step/horizon assertions, and early runtime LR gate recomputed at the actual logged step. NEW skills/preparing-runtime/SKILL.md + index row — explicit interpreter/env, noninteractive progress controls, resumable pinned caches/staging, disk/cache growth/stall checks, enforced log cap, and atomic generation/lease-bound result envelope; installing-libero-headless layers its task-specific prompt/CMake recipe on this generic contract. detaching-gpu-jobs now binds PID/PGID/start identity to the execution lease and durable result envelope. Updated codebase_overview.md and scripts_overview.md.}
{Review/validation: required Devils-Advocate and official-source research passes corrected work-deadline vs handoff-deadline semantics, cross-run registry scope, frozen-attempt lease identity, and scheduler-state-aware LR prediction. simplify:local made no edits. code_review:local found 3 required issues; all applied (expected LR at actual logged step, honest same-user default/host-shared override + occupancy verification, actively enforced log cap). git diff --check passed; quick_validate.py passed for continuing-training, preparing-runtime, detaching-gpu-jobs, and installing-libero-headless.}
{Achieved: yes. Gaps: the default lease registry coordinates concurrent runs for one OS user; multi-user GPU hosts must configure a securely permissioned host-shared coordinator through ROBOT_TRAINING_LEASE_REGISTRY. The new preparing-runtime skill remains lifecycle candidate until exercised in a logged loop round.}
{=============================Function Update===============================}
{Hardening re-port: canonical objective schema + preflight, noise-aware paired scoring, derived CHAMPION, library lock, metadata frontmatter, generation-owned detach — 2026-07-13 10:54 — ID 3}
{Re-derived the 2026-07-10 hardening (lost in the 2026-07-12 durable-recovery rewrite; no source file survived) from update_logs ID 2 / repo_info / run evidence, and ported it into the current durable flow plus the strongest findings from the two real LIBERO runs.}
{Repos involved: agentic_training_loop (this repo); references the external LeRobot repo and the nested .github/HarnessFlow pack.}
{Request: mode-fast code workflow implementing 8 validated findings — (1) objective schema canonical + Step 0 validation + [command preflight] + min_delta/workdir/allowed_executables/preflight_eval_command; (2) fix the broken CHAMPION dependency (durable score_state canonical, pointer derived transactionally); (3) skill frontmatter metadata-nesting so quick_validate.py passes; (4) machine-specific entry-point paths (resolved externally before implementation — dropped); (5) generation-owned detached processes; (6) run-record findings (paired McNemar/bootstrap, gain-per-GPU-hour ranking, inference-first probing, concurrent CPU diagnosis, best_observed, preflight_eval_command); (7) serialize shared skill-library updates; (8) adopt the index registration policy.}
{Implementation: robot_agentic_training_flow.md — 4 new optional [inputs] rows + canonical-schema clause; new [noise band] (Agresti-Caffo two-sample 95% half-width, p-tilde=(x+1)/(n+2), denominators n+2; min_delta replaces), [best observed] (advisory, never drives exit/promotion), [champion pointer] (derived, regenerated from the applied score snapshot on any metric_best/best_checkpoint change and unconditionally at recovery) definitions; [command preflight] gate scoped to the objective's raw command text (detach wrapper exempt) run at Step 0 + before every dispatch; Step 0 renumbered 1-7 with schema validation + preflight + workdir; Step 1 split 1a preflight probe (~1/10-budget minimal checkpoint, degenerate/metric checks via preflight_eval_command with confirm-before-halt) / 1b initialize / 1c trained baseline; Step 3(d) expected-gain-per-GPU-hour rubric + inference-first probing + default-on concurrent CPU diagnosis; Step 3(g) confirmation protocol pre-commit (loop back to 3(f) for one bound confirmation attempt before any snapshot — Invariant 8 intact), pooled re-test, determinism switch to paired McNemar (exact binomial b+c<25, continuity-corrected chi-square otherwise, inconclusive b+c<6) with index-for-index pairing verification, paired bootstrap for non-binary metrics, explicit no-per-episode fallback (NO PROGRESS + steer to min_delta), best_observed update, post-SCORE_APPLIED champion regeneration; Step 3(h) reusable-only registration + controller-held flock on stable skills/LIBRARY.lock (acquired last after LOOP_LOG.lock/ROUND_IN_PROGRESS.lock; re-read index, semantic dup detection, atomic temp+rename); Invariant 5 rewritten to the index policy; recovery item 2 applies best_observed + re-derives CHAMPION; ledger header +Workdir/preflight lines; round entry +band/confirmation +Best observed; noise-aware verdict definitions; [commit key] notes confirmation generation/digest as keyed fields. objective.example.yaml — preflight_eval_command documented; min_delta determinism note. skills/index.md — REGRESSION row to durable score_state; metadata-nested template + rationale; registration-as-library-transaction rule; curating text metadata.lifecycle. All 16 skills migrated to metadata: lifecycle/safety (17/17 pass quick_validate.py; also fixed tuning-action-horizon's masked angle-bracket description violation). reverting-to-champion — durable state authoritative, pointer cross-check, stop-on-mismatch. continuing-training — rollback points at score_state. detaching-gpu-jobs — exit-code sentinel wrapper, records pid/ps-read pgid/proc starttime/cmd sha256/wait generation, identity-verified kill -- -PGID from the controller. Reviews: simplify(local) no changes; code-review(local) 3 Required + 3 nits applied (pairing verification, READY_TO_SCORE ordering reconciliation, wrapper-text mismatch, 52->88 example, commit-key clarification, PGID comment).}
{Achieved: yes. Gaps: detaching-gpu-jobs lifecycle stays candidate despite round-0 evidence (promotion is flow Step 3h evidence curation, not this workflow's call); finding 4 (stale /Users paths) was fixed externally mid-session and dropped from scope; concurrent external spec additions (Invariant 9/10, execution leases, resume preflight, preparing-runtime skill) were preserved untouched and are not covered by this entry.}
{=============================Execution Update===============================}
{write-readme skill + README.html generation + 2026-07-13 14:49 + Execution ID 4}
{Created the repo's first README.md via the pack's write-readme skill, then a self-contained README.html rendering for readability.}
{Repos involved: agentic_training_loop (this repo). Multi-layer context: enclosing umi_vla layer's codebase_overview.md read; its scripts_overview.md and all sibling layers (lerobot, openpi, Isaac-GR00T, progress_estimator, lerobot-annotate, FastUMI_*) skipped as not relevant (>5 layers discovered, bounded per contract).}
{Request: mode fast exec — "create a readme for the repo" using the readme skill, then "create a html version of it"; headers simplify: local, code_review: local, subagent_model: inherit (main model), subagent_effort: high (not-applied — ad-hoc spawns expose no per-invocation effort control).}
{Commands/Skills executed: write-readme (main agent; 8-part layout; all commands/imports verified by execution — quick_validate.py CLI, python3 -m scripts.package_skill module form required, validate_skill/parse_skill_md(Path) snippet); scratchpad md2html.py (python-markdown 3.10.2, extensions tables/fenced_code/toc/sane_lists; <!doctype html> + .markdown-body + 980px/45px + prefers-color-scheme dark; pandoc absent); Step-3 Devils Advocate + Online Researcher subagents (DA retried once after a session-limit API error, then completed; 9 live sources fetched incl. agentskills.io, anthropics/skills, huggingface/lerobot — all verified live); Step-6 simplify:local (1 edit) then code_review:local (6 findings: recovery//tests/ falsified three "no code" claims, SKILL.md became a discovery wrapper, eval_g<m> naming, invented --auto-refresh flag, improve_description params, setsid-nohup nit — all applied).}
{Result: success — README.md (249 lines) + README.html (33KB) at repo root; validations green (all ToC anchors/headings resolve, all relative link targets exist, no invented flags after fixes). A concurrent working-tree change at 14:35 (recovery/ + tests/ added, root SKILL.md replaced by a 10-line wrapper, flow file updated to reference replay_checker) was detected during review, independently re-verified, and folded into the README; drift notes prepended to codebase_overview.md and scripts_overview.md.}
{Achieved: yes. Gaps: upstream comment {eval_dir} -> round_<n>/eval in objective.example.yaml:53 and skills/measuring-the-gate/SKILL.md:29 disagrees with the flow's authoritative round_<n>/eval_g<m> definition (README follows the flow; upstream files left untouched — out of scope for this request).}
{=============================Refactor Update===============================}
{Single authoritative skill entry point + executable durable-recovery checks — 2026-07-13 14:57 — ID 4}
{Removed the competing loop protocol from the Agent Skills entry point and converted the highest-risk durable-state recovery rules into a schema-backed, deterministic replay and partial-append checker.}
{Repo involved: agentic_training_loop (flow specification, discovery wrapper, recovery checker/tests, and nested repo_info documentation).}
{Implementation: replaced root SKILL.md with a 10-line discovery wrapper delegating exclusively to robot_agentic_training_flow.md; added Draft 2020-12 recovery/round_state.schema.json for normalized decoded schema-version-1 state; added pure stdlib recovery/replay_checker.py for known phase ordering, clean round rollover, generation high-water monotonicity/nonreuse, reciprocal measurement/wait bindings, immutable and monotonic wait movement, committed receipt consistency, and byte-anchored partial ledger append recovery; added tests/test_recovery.py covering READY_TO_SCORE through ROUND_ADVANCED, valid/stale rollover, interrupted waits, binding/generation failures, schema agreement, and every nonempty UTF-8 block prefix; linked the decoded-mapping boundary and case-3 helper surgically from the authoritative flow without migrating or automatically gating legacy encodings; updated codebase_overview.md and scripts_overview.md. simplify:local made one documentation-only clarification; code_review:local findings were applied, including clearing execution_leases and phase_transitions on rollover.}
{Achieved: yes. Five focused unittest cases pass (including table/subtest matrices), Python compilation and JSON parsing pass, the Agent Skills validator passes, the wrapper has no OPTIONS.md reference, scoped diff checks pass, and final independent Senior Engineer plus QA reviews are clean. Legacy unversioned Markdown/YAML records intentionally remain governed by the prose protocol until an explicit decoder/migration is introduced.}

{=============================Function Update===============================}
{Self-contained package via context/ vendoring — 2026-07-18 18:58 — ID 5}
{Made the package runnable after `.github/` is gitignored (external dev folder, absent on a fresh clone) by vendoring the flow's runtime dependencies under `context/` and rewiring all references off the pack.}
{Repos involved: agentic_training_loop (this repo). References the nested `.github/HarnessFlow` dev pack only as the (now dev-only, gitignored) source of the vendored files.}
{Request: mode-fast code workflow — "make the package self-contained: `.github` becomes an external gitignored folder, so move the necessary dependencies to `context/` (select + mirror, not blind copy) and rewire references." headers simplify: local, code_review: local, subagent_model: claude-sonnet-4-6, subagent_effort: xhigh (not-applied — ad-hoc Claude Code spawns expose no per-invocation effort control).}
{Implementation: Mirror — added context/skills/{code-simplification,code-review-and-quality}/SKILL.md (the flow's Step-4 `local` review deps); deleted context/skills/index.md (a stale, drifting duplicate of the shipped repo-root skills/index.md, missing 6+ real skills) and context/.DS_Store. Deliberately NOT re-mirrored: context/_lib/stay_active.md (intentional autonomous adaptation) and context/repo_info/* content (newer than the pack — re-mirroring would have regressed README/tests/recovery documentation and 4 update_logs entries), per the challenge subagent's evidence. Rewire — robot_agentic_training_flow.md: 20 body refs `.github/HarnessFlow/`→`context/` and the Required Context Files comment block from machine-absolute back to repo-relative `context/...` with skills/index.md pointing at the repo-root library. context/_lib/workflow_contract.md: fixed corrupt machine-specific `/home/hangyu/umi_vla/.../.github/HarnessFlow/` self-refs → `context/`, rewrote Pack Path Resolution to a 3-tier context-first order, agents-dir default → context/agents/. CLAUDE.md + AGENTS.md: stripped broken `/home/hangyu/...` absolutes to bare pack-relative, Pack Path Resolution → context-first. README.md + README.html: two functional repo_info pointers → context/repo_info/. Added .gitignore (`.github/`, `.DS_Store`). Reviews: simplify(local) no changes (diff already minimal); code_review(local) 3 findings, all evaluated — (1) whole-`.github/` ignore drops `.github/copilot-instructions.md` (VS Code Copilot entry): the user's explicit choice, kept, tradeoff surfaced; (2) copilot-instructions.md stale paths: file lives inside gitignored `.github/`, out of shipped scope; (3) CLAUDE.md Skills section: dev-harness doc, resolves via tier-2 for devs and is moot for the shipped runtime — none required shipped-runtime changes.}
{Achieved: yes. Verified: every context/<path> the runtime references resolves to an existing file, and no runtime-critical bare-ref dangles once `.github/` is gone. Known tradeoff (user-accepted): gitignoring all of `.github/` also excludes `.github/copilot-instructions.md` (VS Code Copilot's auto-discovered entry point) and any GitHub Actions; the surgical alternative is to ignore only `.github/HarnessFlow/`. Not-mirrored non-runtime bare-refs (`_lib/loop_control.md`, `_lib/absolutize_pack_paths.md`, `agents/code-simplifier.md`, `skills/claude-native-skills-subagents`) are not exercised by the robot loop.}
{=============================Function Update===============================}
{Robot-flow request template + offline request-builder GUI — 2026-07-18 18:59 — ID 5}
{Added a fill-in request template and a self-contained static request-builder HTML page for the standalone robot_agentic_training_flow.md, mirroring the pack's harness_gui.html, surfacing objective.example.yaml's parameters and adding an operator Requirements preflight section.}
{Repos involved: agentic_training_loop (two new repo-root files; docs updated in context/repo_info and .github/HarnessFlow/repo_info).}
{Request: mirror the design of .github/HarnessFlow/harness_gui.html to create an HTML GUI and a request template for robot_agentic_training_flow.md; include the parameters from objective.example.yaml; add a requirements section.}
{Implementation: created robot_agentic_training_flow_request_template.md (headers agent type/subagent_model/subagent_effort/simplify/code_review, deliberately no mode:; read-and-follow imperative + hard constraints; Requirements preflight checklist explicitly NOT parsed by the controller; Objective section carrying only the objective path plus the objective.example.yaml field schema, with objective values never inlined per Invariant 1; optional Starting state). Created robot_agentic_training_flow_gui.html (single self-contained page reusing harness_gui.html CSS/theme/helpers; left panel = flow headers incl. custom-model id; right panel = typed Requirements + Objective fields prefilled with the PushT/ACT example, comparison dropdown, numeric target/stagnation, command textareas, optional min_delta/preflight_eval_command/workdir/allowed_executables; live prompt preview [path-only, Invariant-1-safe] + generated objective.yaml preview; clipboard-with-execCommand-fallback copy and Blob downloads; offline hand-rolled YAML — yamlScalar single-quote-doubling, yamlBlock literal-pipe with a uniform 2-space indent, yamlNum YAML-numeric incl. signed-exponent normalization). Design informed by parallel Devils Advocate + Online Researcher subagents; simplify(local) found no changes; code_review(local) found 1 Critical (yamlBlock could emit invalid YAML for inconsistently-indented commands) + 1 Optional (yamlNum exponential parsed as a string) + 2 content-drift + 1 nit — Critical and Optional fixed and re-verified with PyYAML, content-drift fixed by aligning the generated prompt with the template, nit kept as a defensive guard.}
{Achieved: yes. Verified: PyYAML round-trips the generated objective.yaml across the example, adversarial scalars, exponentials, the pathological indentation case, and required-only docs; node --check passes the GUI script; every JS-referenced element id exists in the DOM; the prompt never contains objective values. No gaps.}

{=============================Function Update===============================}
{Deterministic control-plane reference implementation + acceptance-gate spec + skill-evidence tiers + project/CI infra — 2026-07-18 22:16 — ID 6}
{Added a small executable `agentic-robot` control-plane package (validation, scoring, atomic-write event ledger, versioned state, recovery replay) plus objective.schema.json, a documented three-eval-layer/metric-vector acceptance gate, stronger skill-evidence tiers, and standard project/CI hygiene — implementing the low-risk subset of four proposed functionalities and rejecting the risky ones.}
{Repos involved: agentic_training_loop (this repo, working dir /Users/hangyu/Desktop/AgenticRobotics).}
{Request: mode-fast code workflow — "validate and implement, reject risky updates" across four proposals: (2) separate optimization from a hidden acceptance gate with three eval layers + a metric vector, integrating vla-eval/LIBERO-PRO; (3) a small executable reference CLI + schemas + versioned state + migrations + phase schemas + JSONL/SQLite ledger + crash injection + property tests; (7) stronger skill-evidence tiers/metadata; (8) pyproject + CI + LICENSE/CONTRIBUTING/SECURITY/citation + LeRobot compat matrix. Headers simplify: local, code_review: local, subagent_model: claude-sonnet-4-6, subagent_effort: xhigh (not-applied — ad-hoc Claude Code Task spawns expose no per-invocation effort control).}
{Implementation: ACCEPTED (built) — objective.schema.json (Draft-2020-12 mirror of the prose objective contract, additionalProperties:true, optional acceptance metric-vector + acceptance_eval holdout blocks); package agentic_robot/{__init__,objective,scoring,state,ledger,cli}.py (deterministic, read-only/scaffold-only CLI validate|init|status|replay|report|resume; ledger writes only events.jsonl + stdout, never LOOP_LOG.md; atomic_write with crash-hook boundaries); recovery/__init__.py; pyproject.toml (console entry + pinned dev extras + testpaths=tests); .github/workflows/ci.yml (compile/ruff/mypy/pytest, Py 3.10–3.12); .gitignore narrowed to .github/HarnessFlow/ so .github/workflows is tracked; tests test_objective_and_scoring/test_ledger_crash(crash injection at every prepare/write/fsync/rename boundary)/test_cli_integration(fake train/eval)/test_replay_properties(hypothesis idempotence + generation-non-reuse)/test_schemas_and_skills; objective.example.yaml commented three-layer + acceptance block (smoke=preflight_eval_command, selection=eval_command, acceptance=new locked holdout); skills/index.md Evidence-tiers ladder + per-skill evidence-record fields + unattended-selection rule + template evidence_tier; LICENSE(Apache-2.0)/CONTRIBUTING.md/SECURITY.md/CITATION.cff/COMPATIBILITY.md(LeRobot v0.6.0 pin, verified released 2026-07-06). REJECTED as risky/over-engineered (validated against philosophy + repo identity) — actually integrating vla-eval (Docker 3.3–36GB+GPU) and LIBERO-PRO suites (referenced via eval_command instead); a `run`/`resume` that executes training (LLM's job, README:119); SQLite ledger (JSONL chosen); a migration framework for a single schema version (identity seam only); duplicating phase validation into JSON-Schema if/then conditionals (recovery/replay_checker.py already the single source); mass-rewriting all 21 skills' bodies; executing a real LeRobot compat matrix in CI (documented pin only). Step-3 parallel Devils-Advocate + Online-Researcher subagents shaped the plan (avoided a smoke_eval/preflight_eval_command collision and a second LOOP_LOG.md writer; confirmed Apache-2.0 safe). Step-6 simplify:local (1 import-hygiene edit) then code_review:local (6 findings: 2 Critical command_preflight bypasses [added `|` to forbidden metachars; hardened redirection-target check against traversal + sibling-prefix], 2 Required [.gitignore blanket-.github regression removed; added `|`/traversal test cases], 1 Optional [short-write loop in atomic_write/append_event], 1 FYI [parent-dir fsync on first ledger create] — all applied).}
{Achieved: yes. Verified green: ruff + mypy (agentic_robot), python -m compileall (agentic_robot recovery tests), pytest -q = 42 passed (incl. hypothesis property tests + the pre-existing recovery suite), and `agentic-robot validate objective.example.yaml` OK. Concurrent external work reorganized skills/ into original/ and iterated/ mid-flow; the skill-frontmatter test was made recursive + non-vacuous and README/index links re-verified clean. Documented gaps (deliberate, low-risk): the acceptance gate + evidence tiers are spec/schema conventions the LLM controller must honor at runtime — not yet enforced by the flow's Step-0/Step-3 prose (objective.example.yaml remains the canonical schema; objective.schema.json is enforced only by the CLI); vla-eval/LIBERO-PRO are referenced, not wired; the LeRobot matrix is documented, not executed.}

{=============================Function Update===============================}
{New-behavior RL + self-generated-data skills, enforced held-out acceptance gate + measurement-integrity monitor, backend-agnostic framing — 2026-07-19 15:28 — ID 7}
{Analyzed four external advice items and implemented the correct-and-scoped versions while rejecting the risky maximalist ones, via the mode-fast code workflow (subagent_model claude-sonnet-4-6; simplify:local + code_review:local).}
{Repos involved: agentic_training_loop (this repo). References the external LeRobot repo (the new skills drive its lerobot-* CLIs) and the nested .github/HarnessFlow pack (workflow).}
{Request: "analyze the following advice, update the correct ones, reject risky ones": (1) no new-behavior discovery (SFT+config is demo-bounded) → enable RL, relax setups; (2) no self-generated data at scale → add a data-collection skill (search GitHub for a 1000★+ agentic repo), collect rollouts + env rewards, record + human-label when no reward; (3) no held-out/monitor anti-cheating layer → implement it; (4) unbind the repo from LeRobot/SmolVLA (reference bench, not the only target).}
{Implementation: (1) NEW skills/iterated/reinforcing-with-rl/SKILL.md (candidate/mutating) — RL fine-tune against an env reward to escape the demonstration distribution; cites LeRobot-native HIL-SERL/TDMPC (`.[hilserl]`, lerobot.rl.learner/actor, RewardClassifierConfig) + SimpleVLA-RL (2509.09674)/RLinf-VLA/RIPT-VLA as sim GRPO/PPO refs; gated on the acceptance gate to resist reward-hacking. (2) NEW skills/iterated/harvesting-rollouts/SKILL.md (candidate/mutating) — autonomous policy-rollout data + reward logging via `lerobot-rollout --strategy.type=sentry` (the requested 1000★+ repo is LeRobot itself, 26k★), reward stored per the LeRobotDataset schema, no-reward→record+label (annotating-data/scoring-with-reward-model); distinct from teleop collecting-data. Both registered in skills/index.md (Skills table +2 rows; NO-PROGRESS@stagnation routing extended). (3) robot_agentic_training_flow.md — NEW [acceptance gate] made PART of the [exit condition] (opt-in when the objective sets acceptance_eval/acceptance): the run does not exit until BOTH the selection metric AND a locked holdout metric-vector pass (Dwork reusable-holdout basis; reuses scoring.py::acceptance_passed), read only at Step 4, never for action selection; Invariant 2 clause makes a met-metric/failed-acceptance state below-objective (keep looping toward generalization) with no contradiction; new exit_status ACCEPTANCE_FAILED (distinct from REJECTED/REMEASURE_ONLY) starting a normal generalization round; exit-eligible (Step 3i, selection metric) vs exit-confirmed (Step 4, full conjunction) threaded through the gates intro / Step 2 already-satisfied / Step 3i; NEW [measurement integrity] gate (Step 0 pins repo/run-local eval-script sha256; Step 3f verifies it + scans the worker's compact result for eval/harness tampering → verdict ERROR; EvilGenie/METR-informed) scoped to the net-new eval-code case (not duplicating Invariant 3 / [integrity check]); new [acceptance_eval]/[acceptance] [inputs] rows + Step 0 both-or-neither validation + command-preflight coverage; exit-report Acceptance line + durable acceptance_result field. objective.example.yaml — enforcement-pointer comment above the pre-existing acceptance block. This closes update_logs ID 6's documented gap ("acceptance gate ... not yet enforced by the flow's prose"). (4) frontmatter + a new "Backend independence" note + the [run_dir] row + Step 2 degenerate-signal reworded so LeRobot/SmolVLA/LIBERO are the reference/validation backend (contract = a resumable trainer + an eval_command writing eval_info.json), not the only target; a non-LeRobot backend supplies equivalents. README backend-framing + Highlights (acceptance gate/measurement integrity) + skill count 22→24 (16 candidate) + 2 table rows. REJECTED as risky/over-engineered (per philosophy): building an RL engine / data pipeline in-repo; vendoring an external repo's code; cryptographic attestation / rewriting recovery+CLI Python; a mass de-lerobot rewrite of the 22 skills (would destroy working evidence). Step-3 parallel Devils-Advocate + Online-Researcher subagents shaped the plan; Step-6 simplify:local (3 redundancy trims) then code_review:local (1 Critical + 2 Required + 5 Optional/Nit) all applied/resolved — the Critical fixed a Step-2 "already satisfied" path that bypassed the new acceptance gate (routed through Step 4).}
{Achieved: yes. Verified: quick_validate.py passes both new skills; the skill-frontmatter + internal-links tests pass; flow-spec [term]s consistent, no dangling refs, no stale acceptance_result/failure_kind references. Deliberate decision recorded: the acceptance parse relies on [measurement integrity] + controller parsing (Invariant 3), not a second verifier (the holdout is never optimized toward). Pre-existing and out of scope (NOT caused by this change): objective.schema.json is absent from the working tree, so agentic_robot/objective.py and 2 schema tests fail; the local interpreter is Python 3.8 vs the repo's 3.10–3.12 target (removeprefix/Callable-subscript), so the agentic_robot/recovery pytest suites cannot run locally (CI runs 3.10–3.12). No Python/recovery/tests changes.}

{=============================Function Update===============================}
{Backend-decoupled skill bodies + policy-class-aware RL recipes + flow-matching advantage conditioning + cross-round skill mining — 2026-07-19 16:31 — ID 8}
{Analyzed three external advice items and implemented the correct-and-scoped versions while rejecting the risky ones, via the mode-fast code workflow (subagent_model claude-sonnet-4-6; simplify:local + code_review:local). Skills-library-only: no changes to robot_agentic_training_flow.md, agentic_robot/, recovery/, or tests/.}
{Repos involved: agentic_training_loop (this repo). References the external LeRobot repo (reference backend) and the nested .github/HarnessFlow pack (workflow).}
{Request: "analyze the following advice, update the correct ones, reject risky ones": (1) the controller loop is a robot-specialized Karpathy Loop; autonomous-ML-research systems are ahead on search strategy (AIDE tree search, GEAR genetic) and documented failure modes — take the good ideas but KEEP active online search for the newest papers; (2) the VLA skills are SFT/hyperparameter-level while SOTA moved to dedicated RL post-training (SimpleVLA-RL, RLinf-VLA, RIPT-VLA, ConRFT, VLA-RFT) and advantage-conditioned RL (RECAP/pi*0.6) — detach the training skills from lerobot/smolvla, delete or update smolvla/lerobot-only skills, adapt to a wider method range; (3) RATs/CaP-Agent0 auto-synthesize skills from rollouts and CaP-RL trains the coding agent, whereas this library is authored/curated — take the good ideas.}
{Implementation: ACCEPTED — (A) Backend decoupling by RESTRUCTURE, taking the user's own "or update" branch rather than "delete": two-layer skill bodies (backend-independent `## Do` contract + labeled `### Reference backend (LeRobot / SmolVLA / LIBERO)`) applied to continuing-training, curating-dataset, tuning-action-horizon, and measuring-the-gate, preserving every verbatim command, flag, code-line citation, and measured number. harvesting-rollouts and reinforcing-with-rl already had that shape (challenge subagent caught the stale file list). skills/index.md gained a *Backend portability* note explaining the shape. (B) reinforcing-with-rl rewritten with a cited recipe table keyed on POLICY CLASS — the central corrected fact is that SimpleVLA-RL and RIPT-VLA are token/autoregressive-only and do NOT run on a flow-matching action expert (SimpleVLA-RL arXiv 2509.09674 states this outright), while piRL (2510.25889, ODE->SDE) and VLA-RFT (2510.00406, learned variance head) are the published flow-matching remedies; plus an explicit note that only the LeRobot-native row is Executor work and every external row is Implementer work. NEW skills/iterated/conditioning-on-advantage/ (candidate/mutating) — RECAP/pi*0.6 (2511.14759): fit a distributional value model on Monte-Carlo returns, binarize the advantage at a threshold calibrated to ~30% positive, condition the policy on an "Advantage: positive/negative" text token with 30% dropout under the STOCK flow-matching loss, serve the positive bucket (optional CFG beta 1.5-2.5) — needs no action log-probs, so it is the SmolVLA-shaped RL lever. (C) researching-online gained SWEEP MODE (proactive newest-methods search classifying each method applicable-now / applicable-but-unverified / not-applicable, emitting candidate-skill proposals) plus guidance that prompt-supplied arXiv ids are claims to verify and that the published loop failure modes (reward hacking, evaluator overfitting, search collapse, overstated success) are search targets too. (D) NEW skills/iterated/synthesizing-skills-from-rollouts/ (candidate/reversible) — mines >=5 completed ledger rounds + evidence envelopes for cross-round procedures/traps invisible to per-round Step 3(h); >=2-round recurrence bar, max 3 proposals per invocation, prefers folding into an existing skill, registers NOTHING (controller applies under LIBRARY.lock). skills/index.md also gained RATs-derived numeric lifecycle thresholds (>=3 rounds/>=50% improving -> validated; >=10/<=20% -> deprecated), a ~5-round curation cadence, and an explicit precedence rule that evidence_tier beats lifecycle for unattended selection. REJECTED as risky — (i) an `exploring-in-parallel` K-arm search skill: BLOCKING per the challenge subagent, since [train_dir] is a single lineage (flow:101), only the controller may allocate a measurement generation under ROUND_IN_PROGRESS.lock (flow:189,273), and Step 3d is "never delegated" (flow:258), so it needed either a flow-file change or a second measurement path bypassing Invariant 3 — and the research subagent independently confirmed AIDE and GEAR run FULL evaluations per candidate with no cheap pre-screening, so its premise was wrong; the serial explore/measure/keep-or-revert pattern already exists via rebasing-on-a-stronger-checkpoint + reverting-to-champion + souping-task-vectors. (ii) DELETING any lerobot/smolvla-coupled skill — the flow declares LeRobot/SmolVLA/LIBERO the reference AND VALIDATION backend (flow:66), so those procedures must keep working; index.md §Curating also forbids deletion. (iii) CaP-RL (GRPO-training the coding agent) — out of scope; this repo has no training stack for its own orchestrator. (iv) Renaming the lifecycle vocabulary to RATs' experimental/verified/deprecated — semantically identical to candidate/validated/deprecated, so pure churn across every SKILL.md and every historical ledger entry. (v) Porting durability onto Temporal/LangGraph, and adding a second benchmark or RoboArena-style real-world eval — not requested, and benchmark choice is operator-owned via the immutable objective. Step-3 parallel Devils-Advocate + Online-Researcher subagents materially reshaped the plan (the BLOCKING item above, the stale file list, and ~8 corrected facts including the recipe/policy-class matrix and the replacement of an unverified "17.3->91.7" figure with the confirmed 91.0->99.1 / 86.5->98.5). Step-6 simplify:local (1 duplication trim in curating-dataset) then code_review:local (3 SHOULD-FIX + 1 NIT) — all three SHOULD-FIX applied: restored the dropped LeRobot AGENT_GUIDE.md §6/§7 pointer, resolved the Executor-vs-Implementer ambiguity for non-reference RL recipes, and reconciled the new lifecycle thresholds with the pre-existing evidence_tier ladder.}
{Achieved: yes. Verified: the skill-frontmatter (name==directory) and internal-link tests pass; the code-review subagent live-fetched and confirmed every load-bearing citation (2606.19419 RATs thresholds verbatim from its Appendix A.3, 2606.04233, 2511.14759, 2509.09674, 2505.17016, 2510.25889, 2510.06710, 2603.22435); no Invariant violations and no second-measurement-path residue found. Pre-existing and NOT caused by this change (unchanged from ID 7): objective.schema.json is absent from the working tree (deleted in commit ce1b7c8), so 2 schema tests fail; the local interpreter is Python 3.8 vs the repo's 3.10-3.12 target, so the agentic_robot/recovery suites cannot run locally (CI runs 3.10-3.12). One NIT accepted without change: VLA-RFT's mechanism and RLinf-VLA's backbone list could not be confirmed from abstracts alone, which the skill's own "verify against the installed commit" caveat already covers.}

{=============================Function Update===============================}
{Robotic knowledge base — 10-category indexed reference library under context/RoboticKnowledges/ — 2026-07-19 17:11 — ID 9}
{Created a dated, live-web-sourced robotics reference library: knowledge_index.md plus one README.md in each of ten category subfolders, compiled under an anti-fabrication contract that prefers explicit gaps over force-fitted citations.}
{Repos involved: agentic_training_loop (this repo) only. Docs-only; no external repo touched.}
{Request: mode-fast code workflow — "create a robotic knowledge base for the repo under context/RoboticKnowledges, each category in its own subfolder, indexed by knowledge_index.md", with ten categories: (1) ROS1/ROS2 one-sentence intros + searched known issues/challenges/common mistakes; (2) robots and hardware, pure descriptions and parameters only, no advertisement; (3) RL, (4) Imitation Learning, (5) Diffusion and flow matching, (6) VLA, (7) Classic control and planning, (8) kernel methods — each a brief intro plus 10 classic papers (2014-2024, one per year, preferring ICRA/IROS/RSS/CoRL best papers), 3 from 2025, and 5 from 2026 from big labs (Stanford/UCB/CMU/MIT/Tsinghua/Alibaba/NVIDIA/Google/DeepMind, plus PI for VLA), each paper in three sentences (title, key contribution, key results); (9) mechanical engineering, brief; (10) Agentic AI — newest papers, famous skill GitHub repos, newest OpenAI and Anthropic tech reports. Headers simplify: local, code_review: local, subagent_model: claude-sonnet-4-6, subagent_effort: xhigh.}
{Implementation: 11 new Markdown files, ~1,505 lines, all under context/RoboticKnowledges/ — knowledge_index.md (main-agent authored) + {ros,hardware,reinforcement-learning,imitation-learning,diffusion-and-flow-matching,vla,classic-control-and-planning,kernel-methods,mechanical-engineering,agentic-ai}/README.md. No existing file modified except the three repo_info docs. Shared entry format for the six paper categories: bold linked Title - Venue Year, then `award:`/`lab:` markers, then a one-sentence Contribution and a one-sentence Results; every file ends in Coverage notes + Sources. ANTI-FABRICATION CONTRACT (the load-bearing design decision, added after the Step-3 Devils-Advocate finding that a rigid per-year/per-award/per-lab quota structurally pressures fabrication): live-web-only sourcing, every entry cites a fetched URL, `award: <exact name>` only where a fetched page states it else `award: not verified`, `lab:` only where confirmed, `precursor (...)` labels for works predating a subfield, and "no qualifying paper exists for year Y" declared an explicitly VALID and PREFERRED outcome over force-fitting. SCOPE NOTE added to the index and both overviews: unlike its siblings under context/, this tree is NOT a pack mirror and is NEVER read by robot_agentic_training_flow.md at runtime (Devils-Advocate finding: context/ is documented as vendored runtime deps). DUPLICATION AVOIDED (second Devils-Advocate finding): reinforcement-learning/ and vla/ cross-reference skills/iterated/reinforcing-with-rl/SKILL.md as the source of truth for VLA-RL recipes instead of forking its paper table; code review confirmed the paper sets are disjoint and non-contradictory. DEVIATION RECORDED: Step 5 says the main agent implements directly, but the ten category files were authored by ten parallel research-writer subagents (~370 live fetches would have exhausted the controller context, which philosophy.instructions.md forbids); the main agent authored knowledge_index.md and validated all ten files. Step-3 parallel Devils-Advocate + Online-Researcher subagents materially reshaped the plan — the researcher established the venue ground truth that IROS 2026 (Sep 27-Oct 1, Pittsburgh) and CoRL 2026 (Nov 9-12, Austin) HAVE NOT YET HAPPENED, that ICRA 2026 (Vienna, Jun 1-5) has only finalists posted, and that RSS 2026 (Sydney, Jul 13-17) concluded 6 days before this run with awards unpublished. Step-6 simplify:local (6 files: normalized the entry template across independently-written files, collapsed a venue-reality block duplicated into every Coverage-notes section down to an index pointer, added a missing header sentence to 3 files; net -2 lines) then code_review:local (1 Critical, 2 Required, 1 Optional; 16 citation checks across all 6 paper categories, 15/16 fully verified by live WebFetch, no fabricated URLs and no title/venue mismatches found). The Critical was REJECTED after main-agent verification and INVERTED: the reviewer claimed diffusion-and-flow-matching/ mis-attributed FP3 (arXiv 2503.08950) by omitting Tsinghua/IIIS, but a direct fetch of arxiv.org/html/2503.08950v1 showed the author affiliation block reads only "Shanghai AI Laboratory / Shanghai Qi Zhi Institute" with Tsinghua appearing solely in acknowledgments — so the diffusion file was correct and imitation-learning/README.md held the error; the fix was applied there instead. Both Required findings (index undercounting ROS known-issues 14 vs actual 16 and migration pitfalls 8 vs actual 9) and the Optional one (a footnote stating the per-row "verified awards" counts are not comparably defined - RL counts wins only, IL and classic-control count wins+finalists) were applied.}
{Achieved: yes, with deliberate and documented content gaps that are properties of reality, not defects. Verified: all 10 index relative links resolve; the #known-limitations anchor resolves; all six paper categories carry a consistent 11-slot year structure; every file has Coverage notes + Sources; no placeholder/bare URLs; hardware/README.md grep-clean of promotional language (a user-specified constraint); per-file entry counts match the index table (RL 19, IL 19, diffusion 13, VLA 18, classic-control 19, kernel 14). GAPS (all disclosed in-file and in the index, none silent): diffusion-and-flow-matching has 6 deliberately empty pre-2023 year-slots because diffusion robot policies begin with Diffusion Policy at RSS 2023, with 2015/2020/2022 filled by labelled generic-ML precursors; vla has 7 labelled precursor years and 1 empty year because the paradigm begins at RT-1 (2022) and is named by RT-2 (2023); kernel-methods filled 0 of the 5 requested 2026 big-lab slots (genuine 2026 kernel/GP-robotics papers exist but from UTS, TU Munich and unaffiliated authors, listed transparently rather than counted) and had the best-paper preference dropped by design since kernel work lands in IJRR/T-RO/ICML rather than winning flagship-conference awards; NO 2026 entry anywhere carries a confirmed award, only verified finalist status, because IROS and CoRL 2026 have not occurred and ICRA/RSS 2026 winners are unannounced. The request's "10 classic papers, 2014 to 2024, one year each" is internally inconsistent (that range spans 11 years); all six categories resolved it identically as 11 slots. Collection-time limits disclosed in-file: the shared WebSearch budget (200 calls) was exhausted partway through by the ten parallel writers, after which verification continued via direct WebFetch against arXiv/DBLP/Crossref/OpenReview/vendor pages with no prior-knowledge substitution; docs.ros.org, ros.org/reps, iso.org and several vendor PDFs block automated fetching, so a few claims rest on search snippets of the same URL and say so. Award coverage is uneven because only RSS publishes a canonical 2009-2025 best-paper list — absence of an award marker is weak evidence. Subagent model note: subagent_model requested claude-sonnet-4-6, but the Agent tool exposes only tier aliases, so all 13 subagents ran as claude-sonnet-5 (status: fallback-model-mismatch, not a downgrade); subagent_effort xhigh not-applied (ad-hoc spawns expose no per-invocation effort control).}

{=============================Function Update===============================}
{Per-round refine-signal recording step (Step 3j) under LoopRefineRewards/ — 2026-07-19 17:24 — ID 10}
{Added a tenth round-loop sub-step that appends one advisory row per committed round to the repo-root LoopRefineRewards/<run_id>.md, recording which advisory subagent the controller actually used and whether the round moved the metric, for offline refinement of the loop program itself.}
{Repos involved: agentic_training_loop (this repo) only. Spec + docs; no external repo, no Python, no skills-library change.}
{Request: mode-fast code workflow — "during the loop, add one additional recording step, record signals for update loop under LoopRefineRewards (convert to relative path)". Per round record: (0) current round status, current and goal; (1) main agent behavior <=4 words; (2) Focus Analyst suggestions <=4 words; (3) Focus Analyst helpful/used/new-info Yes-No; (4) Free Analyst suggestions <=4 words; (5) Free Analyst Yes-No; (6) Online Researcher results <=4 words; (7) Yes-No; (8) whether this round achieved better performance than the last. Headers: simplify: local, code_review: local, subagent_model: claude-sonnet-4-6, subagent_effort: high.}
{Implementation: robot_agentic_training_flow.md — NEW Step 3(j) "Record" between (i) Log and Step 4, with the standard consumes/gate/writes/actor dataflow line, an 11-column table (Round, Status, Metric -> Target, Main agent, Focus Analyst, Focus used, Free Analyst, Free used, Researcher, Research used, Better) and the once-per-run file preamble template. NEW [refine signal] row in the [outputs] table (LoopRefineRewards/<run_id>.md, one file per run keyed by [run identity], so concurrent runs never contend). NEW refine_signal sub-row in the [state] table — six fields (focus/free/research gist + used flag) persisted at Step 3(d), the only moment all three (c) reports coexist, since Step 3(i) discards worker output as its context-rot defense; every other column is derived at (j) render time. NEW refine_signal_recorded field in the [round-in-progress record]'s named-field list, commit-keyed, reusing Step 3(h) registration's inspect-target/match/no-op idempotency idiom. ORDERING (load-bearing): (j) runs after ledger_committed and BEFORE (i)'s round-advance replacement, because round_log_inputs/score_snapshot are single fields, not per-round maps, and the advance frees the next round's (d)/(g) to overwrite them; recovery step 6 gained a clause completing a missed (j) before advancing, while explicitly never holding the advance for it (an advisory record must not stall the loop). Cross-references updated: Step 3 preamble controller list, Step 1c (3(g-i) -> 3(g-j), round 0 gets a row with all six advisory cells dashed), Step 4's two "Steps 3(h)-3(i)" -> "3(h)-3(j)". README.md: run-outputs sentence (now two in-repo artifacts), flow-file table row, ASCII pipeline diagram. Interpretation recorded: the request's item 7 says "Free Analyst's results" but item 5 already covers Free Analyst and item 6 is the Online Researcher, so item 7 was read as a typo for Online Researcher. DESIGN DECISIONS: no Python — the loop is executed by an LLM reading markdown and agentic_robot/ never runs the loop; markdown table kept over JSONL because nothing parses it back and it matches the existing ledger idiom; in-repo (not run_dir) because the consumer is the cross-run offline revision of the program, which does not weaken Invariant 6. Step-3 parallel Devils-Advocate + Online-Researcher subagents materially reshaped the plan: the challenge caught that the originally-planned "(j) is safe before or after the advance" claim was false and that round-number was a weaker idempotency key than the file's own commit-key discipline (both fixed), and that items 0/1/8 needed no new persisted state (shrinking the sub-object from 7 fields to 6); the researcher supplied arXiv 2605.27621 (introspective LLM judges "fail to faithfully approximate" ablation-based attribution) and self-preference-bias work (2410.21819), which is why the three "used" cells are labelled in-spec as the controller's stated judgment, NOT a measurement, with leave-one-out named as the way to actually measure. REJECTED: dropping the "Better" column as redundant with the verdict — the verdict is gated against metric_best (best-ever, noise-band-filtered) while Better is raw round-over-round movement, so a within-band improvement is NO PROGRESS *and* Better: Yes, and that divergence is exactly what tells an offline reader the noise band is filtering real progress. Step-6 simplify: local (2 comprehension edits: un-nested an aside in Step 1c, added a missing relative pronoun) then code_review: local (3 Required + 2 Nit, all applied) — Required 1 was a real defect in the new text (a crash after the advance destroys (j)'s inputs, since round_log_inputs/score_snapshot are singular), fixed by the mandatory before-advance ordering plus the recovery-step-6 clause; Required 2 added ROUND_IN_PROGRESS.lock around the write (the file is fully rewritten per append, so an unlocked concurrent writer silently drops the other's row); Required 3 widened untrusted-gist neutralization from just pipes/newlines to markdown- and HTML-active characters, since research_gist is one hop from fetched web content.}
{Achieved: yes. Verified: tests/test_schemas_and_skills.py link-resolution and skill-frontmatter tests pass unchanged; the 11 columns in the (j) spec table match the file-preamble header exactly; no "[term]:" line-start link-reference-definition hazard introduced (all new definitions live in table rows, per the file's own warning); every bracketed term used in the new text is defined elsewhere in the file; LoopRefineRewards/ is created by the flow at runtime, so no placeholder file was committed (safety rule 2). Pre-existing and NOT caused by this change (unchanged from ID 7-9): objective.schema.json is absent from the working tree (deleted in commit ce1b7c8), so the same 2 schema tests fail before and after; the local interpreter is Python 3.8 vs the repo's 3.10-3.12 target, so the agentic_robot/recovery suites cannot run locally (CI runs 3.10-3.12). Subagent model note: subagent_model requested claude-sonnet-4-6, but the Agent tool exposes only tier aliases, so all 4 subagents ran as claude-sonnet-5 (status: fallback-model-mismatch, not a downgrade); subagent_effort high not-applied (ad-hoc spawns expose no per-invocation effort control). Label variance: the Devils Advocate returned output_label [valid criticisms] per its own agent definition rather than [challenge report]; content was complete and task-specific, so it was accepted.}

{=============================Function Update===============================}
{Promotion-margin scoring hardening + re-diagnosis on champion change + data-before-recipe + eval-hygiene guards — 2026-07-20 12:36 — ID 11}
{Analyzed four proposed hardening suggestions for the flow's scoring/analysis machinery and implemented the correct-and-scoped versions while rejecting the risky maximalist ones, via the mode-fast code workflow (subagent_model claude-sonnet-5; subagent_effort low; online_researcher_effort medium; simplify:local + code_review:local). Spec + objective-template only — no Python/recovery/CLI/skills-library change.}
{Repos involved: agentic_training_loop (this repo). References the external LeRobot reference backend only via the eval contract; no external repo touched.}
{Request: "implement the following suggestions; reject the risky ones." (1) the promotion rule lets a single noise-spike become a permanent champion — require confirmation/pooled re-eval for any promotion whose margin over the band is small (e.g. delta < 1.5×band, or "promotion needs the paired test to clear α=0.05"), make the paired test gating at the band edge, and set min_delta above the measured same-checkpoint spread, not equal to it; (2) skipping re-analysis after the champion changed cost a round and fired the stagnation gate — make a cheap re-diagnosis mandatory whenever the champion pointer changes, never reuse a pre-promotion diagnosis; (3) move data diagnosis ahead of recipe search; (4) eliminate evaluation leakage and campaign-state drift.}
{Implementation: ACCEPTED — (1) robot_agentic_training_flow.md Step 3(g): outright promotion now requires a **1.5× margin** (`|delta| ≥ 1.5 × [noise band]`, or `≥ 1.5 × [min_delta]`); any smaller directionally-improving delta (within the band, or beyond it but under 1.5×) is routed into the pre-existing confirmation protocol (pooled re-test, or the gating paired McNemar-exact/χ²/bootstrap test at α=0.05) before it can promote — so a single band-edge spike no longer becomes a permanent champion. Mirrored in the [improvement rule] gate (+ a note that a per-round margin bounds one comparison's false-promotion rate, not the cumulative adaptive-reuse selection bias — the [acceptance gate] is that guard), the NO PROGRESS verdict, [best observed], the [min_delta] definition, and objective.example.yaml's min_delta comment (set it above the measured same-checkpoint/test-retest spread). Framing corrected per the challenge: the paired test was ALREADY gating for within-band deltas; the real gap was that beyond-band deltas got ZERO replication, which the margin closes. (2) Step 3(c) skip clause: the analysts may NOT be skipped whenever the previous round promoted a new [best checkpoint] (verdict PROGRESS or EXIT_REVALIDATED; the round-0 baseline excluded) — re-diagnose the new champion (read its per-task/per-episode breakdown from its own eval_info.json when the eval provides one, else a fresh analyst pass) and never reuse a pre-promotion diagnosis. (3) Step 3(d) rubric: a data-before-recipe root-cause clause — when the diagnosis points at the data (systematic label/coverage/distribution failure), fix the data (curating-dataset/annotating-data/collecting-data) before adopting a researched new recipe; a root-cause axis orthogonal to the gain-per-GPU-hour ranking. (4) Step 3(g)(ii): an advisory eval-staleness guard — the champion's stored per-episode outcomes are trusted for the paired test only while the eval is unchanged since it was measured; on a possible [measurement integrity] re-pin, re-measure the champion fresh under the current pin or skip the paired path (verdict stays NO PROGRESS) with a DISTINCT [round entry] note (not (iii)'s "set min_delta"); and [acceptance_eval] now recommends enabling the locked holdout for long/unbounded campaigns (the standing guard against adaptive-holdout selection bias). REJECTED as over-engineered/risky (per philosophy): a durable per-attempt eval-pin-provenance field (the challenge showed #4's "no new state" claim would otherwise be false — so the guard was made degrade-gracefully-advisory instead), per-round champion re-eval, cryptographic eval attestation, a campaign-state tracker, and alpha-spending machinery for cumulative repeated-testing bias (documented as a limitation pointing to the acceptance gate instead of built). Step-3 parallel Devils-Advocate + Online-Researcher subagents materially reshaped the plan: the challenge corrected the #1 "make it gating" framing, showed #2's "per-task profile is free/already parsed" is not spec-backed (only [metric_path] is guaranteed; the read is the analysts' job) → reworded backend-agnostic, caught that the literal #2 trigger would fire on the round-0 baseline → excluded it, and flagged that #4's pin-guard needs state → made it advisory with a distinct note; the researcher confirmed the margin (MDC = SEM·1.96·√2), that the spec's exact-McNemar-under-25-discordant tiering ALREADY matches best practice (so no change there), that arXiv 2606.04233 ("most reported LIBERO gains are not provably statistically significant") backs #1, and that adaptive-holdout reuse (Dwork, 1506.02629) is the true "evaluation leakage" — mitigated by the locked acceptance holdout the spec already has.}
{Achieved: yes. Verified: markdown table-row integrity for every edited [state]/[gates] row (2-col rows = 3 pipes, 3-col row = 4 pipes); no stale "beyond the band → promote outright" phrasing survives; every bracketed [term] used is pre-existing/defined (no new terms introduced); all four skill paths referenced by the new data-before-recipe clause exist on disk; the 1.5× margin is coherent across all touch points (Step 3g bullets, gate 144, verdict 410, [best observed] 105, [min_delta] 88, objective.example.yaml); the pooled re-test keeps its narrower-band pass/fail (no second 1.5× gate) and REGRESSION detection intentionally stays at the 1× band. Step-6 simplify:local (1 punctuation trim in the staleness-guard sentence) then code_review:local (0 Critical/Required; 5 low-severity — 3 applied: restate NO PROGRESS in the staleness fallback, key the #2 trigger on the verdict rather than "changed the [champion pointer]" which is a never-read-back derived artifact, and gloss "sub-margin"; 2 FYI no-change: intentional 1×-band REGRESSION threshold and the still-accurate Status/Better example). Pre-existing and NOT caused by this change (unchanged from ID 7-10): objective.schema.json is absent from the working tree (deleted in commit ce1b7c8), so 2 schema tests fail; the local interpreter is Python 3.8 vs the repo's 3.10-3.12 target and pytest is not installed in it, so the agentic_robot/recovery/schema suites cannot run locally (CI runs 3.10-3.12) — this change touches no Python/schema/skill/test surface, so those outcomes are unaffected. Subagent model note: subagent_model requested claude-sonnet-5; the Agent tool exposes tier aliases, so all 4 subagents ran on the sonnet tier as requested. subagent_effort low / online_researcher_effort medium not-applied (ad-hoc Claude Code Task spawns expose no per-invocation effort control).}
